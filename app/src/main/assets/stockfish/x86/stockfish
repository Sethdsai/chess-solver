#!/system/bin/sh
# Stockfish placeholder - replace with actual binary for production
# The real binary will be downloaded during GitHub Actions CI build
while IFS= read -r line; do
  case "$line" in
    "uci") echo "id name StockfishPlaceholder"; echo "id author StockfishTeam"; echo "uciok" ;;
    "isready") echo "readyok" ;;
    "quit") exit 0 ;;
    go*position*) echo "bestmove e2e4" ;;
    go*) echo "bestmove e2e4" ;;
  esac
done
